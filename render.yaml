services:
  - type: web
    name: github-ai-repo-finder-api
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build:server
    startCommand: node dist/server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: GITHUB_TOKEN
        sync: false
